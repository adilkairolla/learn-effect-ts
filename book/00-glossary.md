# Glossary

> Every term used in the book. Each entry: short definition + chapter where introduced + pattern link.
> Terms starting with `@` or `.` are sorted by the first letter after the prefix.

| Term | Definition | Introduced in | Pattern |
|------|------------|---------------|---------|
| `@effect/ai` | Provider-agnostic AI abstractions: `LanguageModel`, `EmbeddingModel`, `Tool`, MCP integration. | [Ch. 31](part-2-tour/31-ai-core.md) | — |
| `@effect/ai-amazon-bedrock` | Amazon Bedrock provider for `@effect/ai`; multi-model gateway with AWS event-stream encoding. | [Ch. 32](part-2-tour/32-ai-providers.md) | — |
| `@effect/ai-anthropic` | Anthropic Claude provider for `@effect/ai`; wraps the Claude API with streaming and tool use. | [Ch. 32](part-2-tour/32-ai-providers.md) | — |
| `@effect/ai-google` | Google Gemini provider for `@effect/ai`. | [Ch. 32](part-2-tour/32-ai-providers.md) | — |
| `@effect/ai-openai` | OpenAI provider for `@effect/ai`; GPT language models, embeddings, and tool calling. | [Ch. 32](part-2-tour/32-ai-providers.md) | — |
| `@effect/ai-openrouter` | OpenRouter provider for `@effect/ai`; routes to 200+ models via the OpenRouter API. | [Ch. 32](part-2-tour/32-ai-providers.md) | — |
| `@effect/cli` | Framework for building type-safe CLI applications with commands, options, prompts, and auto-complete. | [Ch. 19](part-2-tour/19-cli.md) | — |
| `@effect/cluster` | Actor-model distributed clustering: entity sharding, message routing, cron jobs. | [Ch. 30](part-2-tour/30-cluster.md) | — |
| `@effect/experimental` | Incubator for pre-stable features: `Machine`, `PersistedCache`, `EventLog`, `DevTools`. | [Ch. 44](part-2-tour/44-experimental.md) | — |
| `@effect/opentelemetry` | OpenTelemetry integration; exports Effect spans, metrics, and logs to OTLP. | [Ch. 33](part-2-tour/33-opentelemetry.md) | — |
| `@effect/platform` | Runtime-agnostic interfaces for HTTP client/server, file system, key-value store, and workers. | [Ch. 22](part-2-tour/22-platform.md) | — |
| `@effect/platform-browser` | Browser implementations of `@effect/platform` interfaces using `fetch` and `localStorage`. | [Ch. 24](part-2-tour/24-platform-bun-browser.md) | — |
| `@effect/platform-bun` | Bun runtime implementations of `@effect/platform` interfaces using Bun's native APIs. | [Ch. 24](part-2-tour/24-platform-bun-browser.md) | — |
| `@effect/platform-node` | Node.js implementations of `@effect/platform`: HTTP server/client, file system, subprocesses. | [Ch. 23](part-2-tour/23-platform-node.md) | — |
| `@effect/platform-node-shared` | Internal shared Node.js/Bun primitives reused by platform-node and platform-bun. | [Ch. 23](part-2-tour/23-platform-node.md) | — |
| `@effect/printer` | Wadler/Lindig pretty-printing algebra: `Doc` combinators and page-width-aware layout. | [Ch. 20](part-2-tour/20-printer.md) | — |
| `@effect/printer-ansi` | ANSI terminal color/styling extension for `@effect/printer`. | [Ch. 21](part-2-tour/21-printer-ansi.md) | — |
| `@effect/rpc` | Type-safe RPC framework; derive client and server from a single `RpcGroup` schema definition. | [Ch. 28](part-2-tour/28-rpc.md) | — |
| `@effect/sql` | Core SQL abstraction layer: `SqlClient` service, `SqlResolver`, migrations, and streaming queries. | [Ch. 25](part-2-tour/25-sql-core.md) | — |
| `@effect/sql-clickhouse` | ClickHouse analytics database driver for Effect. | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/sql-d1` | Cloudflare D1 (SQLite-compatible) driver for Effect. | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/sql-drizzle` | Drizzle ORM integration for `@effect/sql`. | [Ch. 27](part-2-tour/27-sql-query-builders.md) | — |
| `@effect/sql-kysely` | Kysely query-builder integration for `@effect/sql`. | [Ch. 27](part-2-tour/27-sql-query-builders.md) | — |
| `@effect/sql-libsql` | libSQL (Turso) driver for Effect. | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/sql-mssql` | Microsoft SQL Server driver using `tedious`; includes stored procedure support. | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/sql-mysql2` | MySQL driver adapter built on `mysql2`. | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/sql-pg` | PostgreSQL driver adapter built on the `postgres` npm package. | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/sql-sqlite-bun` | SQLite driver for Bun using Bun's built-in SQLite API. | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/sql-sqlite-do` | SQLite driver for Cloudflare Durable Objects (`SqlStorage` API). | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/sql-sqlite-node` | SQLite driver for Node.js using `better-sqlite3`; synchronous I/O wrapped in Effect fibers. | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/sql-sqlite-react-native` | SQLite driver for React Native using `expo-sqlite`. | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/sql-sqlite-wasm` | SQLite in WebAssembly with optional OPFS persistence for the browser. | [Ch. 26](part-2-tour/26-sql-drivers.md) | — |
| `@effect/typeclass` | Algebraic typeclass abstractions (`Functor`, `Monad`, `Monoid`, etc.) encoded in Effect style. | [Ch. 42](part-2-tour/42-typeclass.md) | — |
| `@effect/vitest` | Vitest helpers for testing Effect programs: `it.effect`, `it.scoped`, layer management. | [Ch. 43](part-2-tour/43-vitest.md) | — |
| `@effect/workflow` | Durable workflow engine with Activities, automatic retry/idempotency, and `@effect/sql` persistence. | [Ch. 29](part-2-tour/29-workflow.md) | — |
| `.from*` family | Pattern for constructors that convert existing iterables/collections into Effect data types (e.g., `Chunk.fromIterable`). | [Ch. 11](part-1-foundations/11-constructors.md) | [`.from*` family](../research/02-patterns-catalog.md#from-family) |
| `.make` / `.of` constructors | Naming convention for primary typed constructors across Effect modules (e.g., `Ref.make`, `Chunk.of`). | [Ch. 01](part-1-foundations/01-why-effect.md) | [`.make` / `.of` constructors](../research/02-patterns-catalog.md#make--of-constructors) |
| `Brand.nominal` / `refined` / `all` | Nominal typing utilities: `nominal` for cast-only brands, `refined` for validated brands, `all` for composing them. | [Ch. 13](part-1-foundations/13-branded-types.md) | [`Brand.nominal` / `refined` / `all`](../research/02-patterns-catalog.md#brandnominal--refined--all) |
| Branded types | Nominal type wrappers that make structurally identical primitives type-incompatible (e.g., `UserId` vs `ProductId`). | [Ch. 13](part-1-foundations/13-branded-types.md) | [`Brand.nominal` / `refined` / `all`](../research/02-patterns-catalog.md#brandnominal--refined--all) |
| Bridging Option/Either ↔ Effect | Idiom for lifting `Option`/`Either` into an `Effect` via `yield*`, and wrapping Effect outcomes into `Option`/`Either`. | [Ch. 12](part-1-foundations/12-option-and-either.md) | [Bridging Option/Either ↔ Effect (yield*, option, either)](../research/02-patterns-catalog.md#bridging-optioneither--effect-yield-option-either) |
| `Cache.make` / `ScopedCache.make` | Effect-based memoization with capacity and TTL eviction; deduplicates concurrent lookups for the same key. | [Ch. 26](part-2-tour/26-sql-drivers.md) | [`Cache.make` / `ScopedCache.make` — effect-based memoization](../research/02-patterns-catalog.md#cachemake--scopedcachemake--effect-based-memoization) |
| `Cause` | The full failure model of Effect: `Fail` (typed error), `Die` (defect), `Interrupt` (cancellation), and their compositions. | [Ch. 07](part-1-foundations/07-cause-model.md) | [`Cause` — `fail` / `die` / `interrupt` variants](../research/02-patterns-catalog.md#cause--fail--die--interrupt-variants) |
| Channel | Bidirectional stream primitive underlying `Stream` and `Sink`; used to implement custom stream combinators. | [Ch. 41](part-2-tour/41-stream-deep-dive.md) | [Channel — bidirectional stream primitive (Stream's underlying type)](../research/02-patterns-catalog.md#channel--bidirectional-stream-primitive-streams-underlying-type) |
| Chunk | Typed array container optimized for O(1) append and concatenation; the element type emitted by `Stream`. | [Ch. 20](part-2-tour/20-printer.md) | [Chunk — typed array container (Stream's element type)](../research/02-patterns-catalog.md#chunk--typed-array-container-streams-element-type) |
| `Config.string` / `integer` / `boolean` / `nested` / `all` | Typed config schema primitives for loading and validating environment variables at startup. | [Ch. 19](part-2-tour/19-cli.md) | [`Config.string` / `integer` / `boolean` / `nested` / `all`](../research/02-patterns-catalog.md#configstring--integer--boolean--nested--all) |
| `ConfigProvider.fromEnv` / `fromMap` / `fromJson` | Config source adapters: `fromEnv` for production, `fromMap` for tests, `fromJson` for structured config files. | [Ch. 28](part-2-tour/28-rpc.md) | [`ConfigProvider.fromEnv` / `fromMap` / `fromJson`](../research/02-patterns-catalog.md#configproviderfromenv--frommap--fromjson) |
| Context | The dependency-injection container; a typed map from `Tag` identifiers to service implementations. | [Ch. 08](part-1-foundations/08-context-and-tags.md) | [`Context.GenericTag` / `Tag` class / `Reference` — tag variants](../research/02-patterns-catalog.md#contextgenerictag--tag-class--reference--tag-variants) |
| `Context.GenericTag` / `Tag` class / `Reference` | The three tag variants: `GenericTag` (string key), `Tag` class (class-based), `Reference` (with default value). | [Ch. 08](part-1-foundations/08-context-and-tags.md) | [`Context.GenericTag` / `Tag` class / `Reference` — tag variants](../research/02-patterns-catalog.md#contextgenerictag--tag-class--reference--tag-variants) |
| `Cron.parse` / `make` and `DateTime.now` / `make` / `format` | Calendar-based scheduling via cron expressions; `DateTime` for testable clock-aware time operations. | [Ch. 34](part-2-tour/34-schedule.md) | [`Cron.parse` / `make` and `DateTime.now` / `make` / `format`](../research/02-patterns-catalog.md#cronparse--make-and-datetimenow--make--format) |
| `Data.struct` / `tuple` / `array` / `Class` / `TaggedClass` | Constructors for structurally equatable value objects; enable value-based key lookup in `HashMap`/`HashSet`. | [Ch. 18](part-1-foundations/18-data-equal-hash.md) | [`Data.struct` / `tuple` / `array` / `Class` / `TaggedClass`](../research/02-patterns-catalog.md#datastruct--tuple--array--class--taggedclass) |
| `Data.TaggedEnum` | Generates typed constructors for every variant of a closed discriminated union. | [Ch. 18](part-1-foundations/18-data-equal-hash.md) | [`Data.TaggedEnum` — discriminated union constructors](../research/02-patterns-catalog.md#datataggedenum--discriminated-union-constructors) |
| `Data.TaggedError` | Creates a yieldable typed error class with a `_tag` discriminant for use in `Effect.catchTag`. | [Ch. 06](part-1-foundations/06-typed-errors.md) | [`Data.TaggedError`](../research/02-patterns-catalog.md#datataggederror) |
| `Deferred` | One-shot async value; a fiber-safe promise that can be fulfilled from another fiber. | [Ch. 36](part-2-tour/36-concurrency-primitives.md) | [`Deferred` — one-shot async value](../research/02-patterns-catalog.md#deferred--one-shot-async-value) |
| Dual ESM/CJS export pattern | Publishing a package with both ESM (`import`) and CJS (`require`) targets via the `exports` field. | [Ch. 21](part-2-tour/21-printer-ansi.md) | [Dual ESM/CJS export pattern](../research/02-patterns-catalog.md#dual-esmcjs-export-pattern) |
| Dual data-first / data-last | API style where a function works both as `f(a, b)` (data-first) and `pipe(a, f(b))` (data-last/curried). | [Ch. 04](part-1-foundations/04-pipe-and-dual-api.md) | [Dual data-first / data-last (`dual(...)`) and Pipeable trait](../research/02-patterns-catalog.md#dual-data-first--data-last-dual-and-pipeable-trait) |
| `effect` (package) | The Effect runtime: `Effect`, `Layer`, `Context`, `Schema`, `Stream`, `Fiber`, `STM`, and the full standard library. | [Ch. 01](part-1-foundations/01-why-effect.md) | — |
| `Effect<A, E, R>` | A description of a computation that may fail with `E`, succeed with `A`, and require services `R`. | [Ch. 02](part-1-foundations/02-effect-as-a-value.md) | [`Effect.succeed` / `fail` / `sync` / `promise` / `tryPromise`](../research/02-patterns-catalog.md#effectsucceed--fail--sync--promise--trypromise) |
| `Effect.acquireRelease` / `acquireUseRelease` | Resource bracket: acquires a resource and guarantees its release on success, failure, and interruption. | [Ch. 10](part-1-foundations/10-layer-scoped-and-scope.md) | [`Effect.acquireRelease` / `acquireUseRelease`](../research/02-patterns-catalog.md#effectacquirerelease--acquireuserelease) |
| `Effect.all` / `Effect.repeat` / `Effect.retry` | Combinators for concurrent execution, repeating, and retrying Effects with a `Schedule`. | [Ch. 05](part-1-foundations/05-effect-gen.md) | [`Effect.all` / `Effect.repeat` / `Effect.retry` — combinators](../research/02-patterns-catalog.md#effectall--effectrepeat--effectretry--combinators) |
| `Effect.catchTag` / `catchTags` / `sandbox` | Error handling: recover from specific tagged errors; `sandbox` exposes the full `Cause` for inspection. | [Ch. 06](part-1-foundations/06-typed-errors.md) | [`Effect.catchTag` / `catchTags` / `sandbox` — error handling](../research/02-patterns-catalog.md#effectcatchtag--catchtags--sandbox--error-handling) |
| `Effect.fn` | Creates a named Effect function with auto-tracing (auto-generates a span without manual `withSpan`). | [Ch. 05](part-1-foundations/05-effect-gen.md) | [`Effect.fn` (named effect functions with auto-tracing)](../research/02-patterns-catalog.md#effectfn-named-effect-functions-with-auto-tracing) |
| `Effect.fork` / `forkDaemon` / `forkScoped` / `forkIn` | Start a concurrent fiber; variants differ in how the fiber's lifetime is scoped. | [Ch. 17](part-1-foundations/17-fibers-and-concurrency.md) | [`Effect.fork` / `forkDaemon` / `forkScoped` / `forkIn`](../research/02-patterns-catalog.md#effectfork--forkdaemon--forkscoped--forkin) |
| `Effect.gen` | Generator-based sequential composition using `yield*` to unwrap Effects, Options, and Eithers. | [Ch. 05](part-1-foundations/05-effect-gen.md) | [`Effect.gen` + `yield*`](../research/02-patterns-catalog.md#effectgen--yield) |
| `Effect.runPromise` / `runSync` / `runFork` | The "end of the world" runners that execute an Effect at application entry points. | [Ch. 03](part-1-foundations/03-running-effects.md) | [`Effect.runPromise` / `runSync` / `runFork`](../research/02-patterns-catalog.md#effectrunpromise--runsync--runfork) |
| `Effect.Service` class | One-declaration pattern that generates a service `Tag`, `Layer`, and interface simultaneously. | [Ch. 08](part-1-foundations/08-context-and-tags.md) | [`Effect.Service` class](../research/02-patterns-catalog.md#effectservice-class) |
| `Effect.succeed` / `fail` / `sync` / `promise` / `tryPromise` | Leaf constructors for lifting plain values, errors, and Promises into the Effect world. | [Ch. 02](part-1-foundations/02-effect-as-a-value.md) | [`Effect.succeed` / `fail` / `sync` / `promise` / `tryPromise`](../research/02-patterns-catalog.md#effectsucceed--fail--sync--promise--trypromise) |
| `Effect.withSpan` / `annotateCurrentSpan` | Wraps an Effect in a named tracing span; `annotateCurrentSpan` adds key-value attributes to the active span. | [Ch. 33](part-2-tour/33-opentelemetry.md) | [`Effect.withSpan` / `annotateCurrentSpan` — distributed tracing](../research/02-patterns-catalog.md#effectwithspan--annotatecurrentspan--distributed-tracing) |
| Either | A type with two cases: `Right<A>` (success) and `Left<E>` (error); used for pure synchronous computations that can fail. | [Ch. 12](part-1-foundations/12-option-and-either.md) | [Either — Left / Right and combinators](../research/02-patterns-catalog.md#either--left--right-and-combinators) |
| Encoding | Base64, hex, and UTF-8 codec utilities with typed `Either` error results. | [Ch. 38](part-2-tour/38-config-and-secrets.md) | [Encoding — Base64 / hex / UTF-8 codecs](../research/02-patterns-catalog.md#encoding--base64--hex--utf-8-codecs) |
| `Equal.equals` / `Hash` | Structural equality interface and hash protocol; implemented by all Effect value types for use in `HashMap`/`HashSet`. | [Ch. 18](part-1-foundations/18-data-equal-hash.md) | [`Equal.equals` interface and `Hash` — structural equality](../research/02-patterns-catalog.md#equalequals-interface-and-hash--structural-equality) |
| Exit | The outcome of a completed Effect: `Exit.succeed(value)` or `Exit.fail(cause)`; used at fiber boundaries. | [Ch. 07](part-1-foundations/07-cause-model.md) | [Exit — Effect outcome value (Success / Failure of Cause)](../research/02-patterns-catalog.md#exit--effect-outcome-value-success--failure-of-cause) |
| Fiber | A lightweight, cooperative concurrent unit of execution; the result of `Effect.fork`. | [Ch. 17](part-1-foundations/17-fibers-and-concurrency.md) | [Fiber — joining, interrupting, racing (Effect.fork return type)](../research/02-patterns-catalog.md#fiber--joining-interrupting-racing-effectfork-return-type) |
| Fiber — joining, interrupting, racing | Operations on a `Fiber` handle: `join` (await result), `interrupt` (cancel), `all` (composite). | [Ch. 17](part-1-foundations/17-fibers-and-concurrency.md) | [Fiber — joining, interrupting, racing (Effect.fork return type)](../research/02-patterns-catalog.md#fiber--joining-interrupting-racing-effectfork-return-type) |
| FiberId | Runtime identity for a fiber (ID + start timestamp); used in observability and debugging tools. | [Ch. 17](part-1-foundations/17-fibers-and-concurrency.md) | [FiberId — fiber identity and lineage](../research/02-patterns-catalog.md#fiberid--fiber-identity-and-lineage) |
| `FiberRef` | Fiber-local state that is inherited by child fibers but isolated from siblings; Effect's analog to `AsyncLocalStorage`. | [Ch. 37](part-2-tour/37-fiber-ref-and-semaphore.md) | [`FiberRef` — fiber-local state](../research/02-patterns-catalog.md#fiberref--fiber-local-state) |
| `FiberSet` / `FiberMap` / `FiberHandle` | Fiber lifecycle trackers: `FiberSet` (collection), `FiberMap` (keyed), `FiberHandle` (single exclusive). | [Ch. 37](part-2-tour/37-fiber-ref-and-semaphore.md) | [`FiberSet` / `FiberMap` / `FiberHandle` — fiber lifecycle tracking](../research/02-patterns-catalog.md#fiberset--fibermap--fiberhandle--fiber-lifecycle-tracking) |
| HashMap | Immutable map using structural equality (`Equal`) for key comparison; enables value-object map keys. | [Ch. 40](part-2-tour/40-immutable-collections.md) | [HashMap — structural-equality keyed map](../research/02-patterns-catalog.md#hashmap--structural-equality-keyed-map) |
| HashSet | Immutable set using structural equality for membership; enables deduplication of value objects. | [Ch. 40](part-2-tour/40-immutable-collections.md) | [HashSet — structural-equality set](../research/02-patterns-catalog.md#hashset--structural-equality-set) |
| Interruption | The structured cancellation mechanism in Effect; fibers can be interrupted, and finalizers always run. | [Ch. 17](part-1-foundations/17-fibers-and-concurrency.md) | [`Effect.fork` / `forkDaemon` / `forkScoped` / `forkIn`](../research/02-patterns-catalog.md#effectfork--forkdaemon--forkscoped--forkin) |
| `JSDoc` `@since`, `@category`, `@example` tags | Documentation conventions used in the Effect monorepo: version metadata, grouping, and usage examples. | [Ch. 22](part-2-tour/22-platform.md) | [`JSDoc` `@since`, `@category`, `@example` tags](../research/02-patterns-catalog.md#jsdoc-since-category-example-tags) |
| Layer | A description of how to build a service (or set of services) from other services; the DI wiring primitive. | [Ch. 09](part-1-foundations/09-layer.md) | [`Layer.succeed` / `effect` / `scoped` — Layer constructors](../research/02-patterns-catalog.md#layersucceed--effect--scoped--layer-constructors) |
| `Layer.merge` / `provide` / `fresh` | Layer composition: `merge` combines side-by-side, `provide` wires dependencies, `fresh` disables memoization. | [Ch. 09](part-1-foundations/09-layer.md) | [`Layer.merge` / `provide` / `fresh` — Layer composition](../research/02-patterns-catalog.md#layermerge--provide--fresh--layer-composition) |
| LayerMap | Keyed map of lazily-initialized layers; enables per-tenant or per-request service isolation. | [Ch. 30](part-2-tour/30-cluster.md) | [LayerMap — keyed map of layers (per-tenant / per-request)](../research/02-patterns-catalog.md#layermap--keyed-map-of-layers-per-tenant--per-request) |
| `Layer.scoped` | Wraps a resource acquisition (via `acquireRelease`) into a layer whose cleanup runs on layer teardown. | [Ch. 10](part-1-foundations/10-layer-scoped-and-scope.md) | [`Layer.scoped` (resource layers)](../research/02-patterns-catalog.md#layerscoped-resource-layers) |
| `Layer.succeed` / `effect` / `scoped` | The three primary layer constructors: for pure values, effectful initialization, and resource-owning services. | [Ch. 09](part-1-foundations/09-layer.md) | [`Layer.succeed` / `effect` / `scoped` — Layer constructors](../research/02-patterns-catalog.md#layersucceed--effect--scoped--layer-constructors) |
| List | Immutable persistent linked list; O(1) prepend (`cons`) and head/tail access; ideal for recursive algorithms. | [Ch. 40](part-2-tour/40-immutable-collections.md) | [List — persistent linked list](../research/02-patterns-catalog.md#list--persistent-linked-list) |
| `Logger.make` / `withMinimumLogLevel` / `Effect.log*` | Custom logger construction and the `Effect.logInfo` / `logError` / `logDebug` family for structured logging. | [Ch. 33](part-2-tour/33-opentelemetry.md) | [`Logger.make` / `withMinimumLogLevel` and `Effect.log*` family](../research/02-patterns-catalog.md#loggermake--withminimumloglevel-and-effectlog-family) |
| `Mailbox` | Ordered message inbox for actor-style programming; supports `done`/`fail` signals to terminate the actor. | [Ch. 32](part-2-tour/32-ai-providers.md) | [`Mailbox` — ordered message inbox](../research/02-patterns-catalog.md#mailbox--ordered-message-inbox) |
| `ManagedRuntime.make` | Creates a long-lived runtime tied to a layer's scope; used in React/Node apps that run Effects repeatedly. | [Ch. 11](part-1-foundations/11-constructors.md) | [`ManagedRuntime.make`](../research/02-patterns-catalog.md#managedruntimemake) |
| `Match.value` / `Match.type` | Entry points for Effect's exhaustive pattern matching: `value` for a runtime value, `type` for a reusable matcher function. | [Ch. 39](part-2-tour/39-match.md) | [`Match.value` / `Match.type` — starting a match](../research/02-patterns-catalog.md#matchvalue--matchtype--starting-a-match) |
| `Match.when` / `not` / `exhaustive` | Match clauses: `when` (pattern → handler), `not` (negation), `exhaustive` (compile-time total coverage check). | [Ch. 39](part-2-tour/39-match.md) | [`Match.when` / `not` / `exhaustive` — clauses and finalizers](../research/02-patterns-catalog.md#matchwhen--not--exhaustive--clauses-and-finalizers) |
| `Metric.counter` / `gauge` / `histogram` / `summary` | Prometheus-style metrics: counters (events), gauges (levels), histograms (latency buckets), summaries (quantiles). | [Ch. 33](part-2-tour/33-opentelemetry.md) | [`Metric.counter` / `gauge` / `histogram` / `summary`](../research/02-patterns-catalog.md#metriccounter--gauge--histogram--summary) |
| Option | A type with two cases: `Some<A>` (value present) and `None` (absent); null-safe without exceptions. | [Ch. 12](part-1-foundations/12-option-and-either.md) | [Option — Some / None and combinators](../research/02-patterns-catalog.md#option--some--none-and-combinators) |
| `pipe` | Applies a left-to-right series of transformations; the functional alternative to method chaining. | [Ch. 04](part-1-foundations/04-pipe-and-dual-api.md) | [`pipe` vs method chaining](../research/02-patterns-catalog.md#pipe-vs-method-chaining) |
| Pipeable | Interface that adds a `.pipe()` method to a type, enabling method-chaining pipeline syntax. | [Ch. 04](part-1-foundations/04-pipe-and-dual-api.md) | [Dual data-first / data-last (`dual(...)`) and Pipeable trait](../research/02-patterns-catalog.md#dual-data-first--data-last-dual-and-pipeable-trait) |
| `Pool.make` / `Pool.makeWithTTL` / `KeyedPool` | Fixed-size or TTL-bounded resource pools; `KeyedPool` for per-host pools (e.g., per-upstream connection pools). | [Ch. 23](part-2-tour/23-platform-node.md) | [`Pool.make` / `Pool.makeWithTTL` and `KeyedPool`](../research/02-patterns-catalog.md#poolmake--poolmakewithttl-and-keyedpool) |
| `PubSub` | Multi-subscriber broadcast hub; every subscriber receives every published message (fan-out). | [Ch. 36](part-2-tour/36-concurrency-primitives.md) | [`PubSub` — multi-subscriber broadcast](../research/02-patterns-catalog.md#pubsub--multi-subscriber-broadcast) |
| `Queue` | Concurrent FIFO queue in four modes: unbounded, bounded (backpressure), sliding, and dropping. | [Ch. 36](part-2-tour/36-concurrency-primitives.md) | [`Queue` — unbounded / bounded / sliding / dropping](../research/02-patterns-catalog.md#queue--unbounded--bounded--sliding--dropping) |
| Random | Injectable, testable random number service; use `Random.make(seed)` for deterministic test sequences. | [Ch. 38](part-2-tour/38-config-and-secrets.md) | [Random — testable seed-based RNG service](../research/02-patterns-catalog.md#random--testable-seed-based-rng-service) |
| RateLimiter | Token-bucket rate limiter; delays Effects to stay within a rate limit (e.g., 100 req/min). | [Ch. 44](part-2-tour/44-experimental.md) | [RateLimiter — token-bucket rate limiting](../research/02-patterns-catalog.md#ratelimiter--token-bucket-rate-limiting) |
| `RcRef` / `RcMap` | Reference-counted resources: `RcRef` for a shared single resource, `RcMap` for per-key resources with TTL eviction. | [Ch. 24](part-2-tour/24-platform-bun-browser.md) | [`RcRef` and `RcMap` — reference-counted resources](../research/02-patterns-catalog.md#rcref-and-rcmap--reference-counted-resources) |
| RedBlackTree | Self-balancing ordered tree; O(log n) insert/delete and range queries; underlying structure for `SortedMap`/`SortedSet`. | [Ch. 40](part-2-tour/40-immutable-collections.md) | [RedBlackTree](../research/02-patterns-catalog.md#redblacktree) |
| Redacted | Wraps any value so its `toString`/`toJSON` output is `<redacted>`; prevents secrets from leaking to logs or spans. | [Ch. 27](part-2-tour/27-sql-query-builders.md) | [Redacted — prevent secret values from leaking to logs/spans](../research/02-patterns-catalog.md#redacted--prevent-secret-values-from-leaking-to-logsspans) |
| `Ref` | Atomic mutable cell; the standard replacement for shared `let` variables between fibers. | [Ch. 36](part-2-tour/36-concurrency-primitives.md) | [`Ref` — atomic mutable cell](../research/02-patterns-catalog.md#ref--atomic-mutable-cell) |
| Reloadable | Hot-reloads a service layer at runtime, either on a schedule (`auto`) or on demand (`manual`). | [Ch. 29](part-2-tour/29-workflow.md) | [Reloadable — hot-reload a service layer at runtime](../research/02-patterns-catalog.md#reloadable--hot-reload-a-service-layer-at-runtime) |
| `Request.of` / `RequestResolver.make` / `Effect.request` | The DataLoader triad for N+1 query elimination: batches concurrent single-item requests into one call. | [Ch. 25](part-2-tour/25-sql-core.md) | [`Request.of` / `RequestResolver.make` / `Effect.request` — request batching](../research/02-patterns-catalog.md#requestof--requestresolvermake--effectrequest--request-batching) |
| Runtime | Pre-built execution environment carrying a `Context`, `RuntimeFlags`, and `FiberRefs`; used to run Effects. | [Ch. 43](part-2-tour/43-vitest.md) | [Runtime — pre-built runtime for executing Effects](../research/02-patterns-catalog.md#runtime--pre-built-runtime-for-executing-effects) |
| RuntimeFlags | Bitset controlling runtime features: cooperative yielding, tracing, interruption, and more. | [Ch. 43](part-2-tour/43-vitest.md) | [RuntimeFlags — concurrency, tracing, interruption controls](../research/02-patterns-catalog.md#runtimeflags--concurrency-tracing-interruption-controls) |
| `Schedule.jittered` / `compose` | Schedule combinators: `jittered` adds random noise to prevent thundering herd; `compose` chains two schedules. | [Ch. 34](part-2-tour/34-schedule.md) | [`Schedule.jittered` / `compose` — combinators](../research/02-patterns-catalog.md#schedulejittered--compose--combinators) |
| `Schedule.spaced` / `exponential` / `fixed` / `recurs` | Core schedule constructors for repeating/retrying: fixed delays, exponential backoff, and N-times recurrence. | [Ch. 34](part-2-tour/34-schedule.md) | [`Schedule.spaced` / `exponential` / `fixed` / `recurs`](../research/02-patterns-catalog.md#schedulespaced--exponential--fixed--recurs) |
| `Schema.brand` / `filter` | Schema constraints: `brand` for nominal typing, `filter` for predicate-based runtime refinements. | [Ch. 15](part-1-foundations/15-schema-part-2.md) | [`Schema.brand` / `filter` — constraints](../research/02-patterns-catalog.md#schemabrand--filter--constraints) |
| `Schema.Class` / `Schema.TaggedClass` | Schema-aware class constructors that validate at construction time and support encode/decode. | [Ch. 14](part-1-foundations/14-schema-part-1.md) | [`Schema.Class` and `Schema.TaggedClass`](../research/02-patterns-catalog.md#schemaclass-and-schemataggedclass) |
| `Schema.decode` / `encode` / `is` | Entry points for running a schema: `decode` (parse → domain), `encode` (domain → wire), `is` (type predicate). | [Ch. 14](part-1-foundations/14-schema-part-1.md) | [`Schema.decode` / `encode` / `is` entry points](../research/02-patterns-catalog.md#schemadecode--encode--is-entry-points) |
| `Schema.Struct` | Primary structural schema constructor for object types; the Schema equivalent of a TypeScript `interface`. | [Ch. 14](part-1-foundations/14-schema-part-1.md) | [`Schema.Struct`](../research/02-patterns-catalog.md#schemastruct) |
| `Schema.transform` / `transformOrFail` | Bidirectional schema conversion between encoded (wire) and decoded (domain) forms, with or without failure. | [Ch. 15](part-1-foundations/15-schema-part-2.md) | [`Schema.transform` / `transformOrFail`](../research/02-patterns-catalog.md#schematransform--transformorfail) |
| `Scope` | The resource-lifecycle container; finalizers registered via `acquireRelease` run in LIFO order when the scope closes. | [Ch. 10](part-1-foundations/10-layer-scoped-and-scope.md) | [Structured concurrency via `Scope`](../research/02-patterns-catalog.md#structured-concurrency-via-scope) |
| `Scope.make` / `Scope.fork` / `Scope.close` | Low-level scope primitives; prefer `Effect.scoped` or `Layer.scoped` in application code. | [Ch. 10](part-1-foundations/10-layer-scoped-and-scope.md) | [`Scope.make` / `Scope.fork` / `Scope.close`](../research/02-patterns-catalog.md#scopemake--scopefork--scopeclose) |
| ScopedRef | Mutable slot for a resource; replacing the value automatically closes the previous resource via its scope. | [Ch. 10](part-1-foundations/10-layer-scoped-and-scope.md) | [ScopedRef — scope-attached mutable reference](../research/02-patterns-catalog.md#scopedref--scope-attached-mutable-reference) |
| Secret | Memory-safe secret string stored as a byte array; minimizes risk of credential exposure in heap dumps. | [Ch. 38](part-2-tour/38-config-and-secrets.md) | [Secret — memory-safe secret string](../research/02-patterns-catalog.md#secret--memory-safe-secret-string) |
| `Semaphore` | Async concurrency limiter; `withPermits(n)` brackets an Effect within N permits (use 1 as a mutex). | [Ch. 37](part-2-tour/37-fiber-ref-and-semaphore.md) | [`Semaphore` — async resource limiting](../research/02-patterns-catalog.md#semaphore--async-resource-limiting) |
| Service | A named capability (interface + `Tag`) injected via the `R` type parameter and provided by a `Layer`. | [Ch. 08](part-1-foundations/08-context-and-tags.md) | [`Effect.Service` class](../research/02-patterns-catalog.md#effectservice-class) |
| Sink | Stream consumer/aggregator; used to fold or drain stream elements (e.g., `Sink.fold`, `Sink.drain`). | [Ch. 41](part-2-tour/41-stream-deep-dive.md) | [Sink — Stream consumer / aggregator](../research/02-patterns-catalog.md#sink--stream-consumer--aggregator) |
| SortedMap / SortedSet | Ordered collections maintaining sort invariant on insert via an `Order`; O(log n) ordered iteration. | [Ch. 42](part-2-tour/42-typeclass.md) | [SortedMap / SortedSet (with Order)](../research/02-patterns-catalog.md#sortedmap--sortedset-with-order) |
| `STM.gen` / `STM.commit` | Software transactional memory: compose atomic multi-variable updates in a generator, then commit as an Effect. | [Ch. 35](part-2-tour/35-stm.md) | [`STM.gen` / `STM.commit` — software transactional memory](../research/02-patterns-catalog.md#stmgen--stmcommit--software-transactional-memory) |
| Stream | A pull-based, lazily-evaluated sequence of values that may be async, effectful, and finitely or infinitely long. | [Ch. 16](part-1-foundations/16-stream.md) | [`Stream.make` / `fromIterable` / `fromEffect`](../research/02-patterns-catalog.md#streammake--fromiterable--fromeffect) |
| `Stream.async*` family | Constructors for push-based (event-driven) stream sources: `asyncPush` for callbacks, `fromAsyncIterable` for async iterators. | [Ch. 16](part-1-foundations/16-stream.md) | [`Stream.async*` family (`asyncPush`, `fromAsyncIterable`)](../research/02-patterns-catalog.md#streamasync-family-asyncpush-fromasynciterable) |
| `Stream.fromPubSub` / `fromQueue` / `fromSchedule` / `groupBy` | Stream constructors from concurrent primitives; `groupBy` partitions into keyed sub-streams. | [Ch. 41](part-2-tour/41-stream-deep-dive.md) | [`Stream.fromPubSub` / `fromQueue` / `fromSchedule` / `groupBy`](../research/02-patterns-catalog.md#streamfrompubsub--fromqueue--fromschedule--groupby) |
| `Stream.make` / `fromIterable` / `fromEffect` | Core stream constructors: from varargs, from an iterable, or from a single-valued Effect. | [Ch. 16](part-1-foundations/16-stream.md) | [`Stream.make` / `fromIterable` / `fromEffect`](../research/02-patterns-catalog.md#streammake--fromiterable--fromeffect) |
| `Stream.paginate` | Unfolds a cursor-based or page-based paginated API into a lazy stream of all items. | [Ch. 16](part-1-foundations/16-stream.md) | [`Stream.paginate`](../research/02-patterns-catalog.md#streampaginate) |
| Structured concurrency | Concurrency model where fiber lifetimes are nested within scopes; child fibers are interrupted when their scope closes. | [Ch. 17](part-1-foundations/17-fibers-and-concurrency.md) | [Structured concurrency via `Scope`](../research/02-patterns-catalog.md#structured-concurrency-via-scope) |
| `SubscriptionRef` | An observable `Ref`; `ref.changes` exposes a `Stream<A>` that emits the current value and every subsequent update. | [Ch. 44](part-2-tour/44-experimental.md) | [`SubscriptionRef` — observable Ref](../research/02-patterns-catalog.md#subscriptionref--observable-ref) |
| Supervisor | Observability hook for the fiber lifecycle: `onStart`, `onEnd` callbacks; `track` collects all live fibers. | [Ch. 31](part-2-tour/31-ai-core.md) | [Supervisor — observe and react to fiber lifecycle](../research/02-patterns-catalog.md#supervisor--observe-and-react-to-fiber-lifecycle) |
| `SynchronizedRef` | Like `Ref` but `modifyEffect` operations are serialized; enables atomic effectful read-modify-write cycles. | [Ch. 44](part-2-tour/44-experimental.md) | [SynchronizedRef — atomic effectful update](../research/02-patterns-catalog.md#synchronizedref--atomic-effectful-update) |
| Tag | A unique identifier for a service in the `Context`; the key that `Layer` provides and `Effect.R` requires. | [Ch. 08](part-1-foundations/08-context-and-tags.md) | [`Context.GenericTag` / `Tag` class / `Reference` — tag variants](../research/02-patterns-catalog.md#contextgenerictag--tag-class--reference--tag-variants) |
| The `internal/` folder and `index.ts` re-export shape | Package layout convention: implementation in `internal/`, public API re-exported via namespaced `index.ts`. | [Ch. 22](part-2-tour/22-platform.md) | [The `internal/` folder and `index.ts` re-export shape](../research/02-patterns-catalog.md#the-internal-folder-and-indexts-re-export-shape) |
| `TRef` / `TQueue` / `TMap` / `TSemaphore` | STM-aware data structure variants for use inside `STM.gen` transactions. | [Ch. 35](part-2-tour/35-stm.md) | [`TRef` / `TQueue` / `TMap` / `TSemaphore` — STM-aware variants](../research/02-patterns-catalog.md#tref--tqueue--tmap--tsemaphore--stm-aware-variants) |
| Trie | Prefix-keyed string trie; `keysWithPrefix(p)` returns all keys starting with `p` in O(prefix length + result count). | [Ch. 40](part-2-tour/40-immutable-collections.md) | [Trie](../research/02-patterns-catalog.md#trie) |
